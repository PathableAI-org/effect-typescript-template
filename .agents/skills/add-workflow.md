# Add Workflow

1. Implement workflows as named `Effect.fn` operations.
2. Depend on service contracts instead of directly using infrastructure.
3. Keep workflow logic deterministic and side-effect boundaries explicit.
4. Add tests that provide test Layers and verify typed success/error behavior.
